Site Configuration
##################

..  include::   /references.inc

The current website is deployed to a Ubuntu 12.04 LTS server managed by
RackSpace_. That server is running Apache2 and the site application is a Flask_
program running under ``mod_wsgi``. The site file is shown here:

..  literalinclude::    ../apache2/website


